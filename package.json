{
  "name": "nodejs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "build": "rm -rf build && tsc --project tsconfig.release.json",
    "dev": "nodemon --watch './**/*.ts' --exec 'ts-node-esm' ./src/server.ts"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "@types/express": "^4.17.17",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.21",
    "ts-node": "^10.9.1"
  }
}